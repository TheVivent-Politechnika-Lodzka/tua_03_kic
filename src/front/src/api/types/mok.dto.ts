export interface AccountEditDto {
  firstName: string;
  surname: string;
  email: string;
  phoneNumber: string;
}

export interface AccountDto {
  login: string;
  firstName: string;
  surname: string;
  email: string;
  phoneNumber: string;
}
