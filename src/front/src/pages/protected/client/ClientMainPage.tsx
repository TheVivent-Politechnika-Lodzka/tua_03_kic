import styles from "./style.module.scss";

const ClientMainPage = () => {
    return (
        <section className={styles.clientMainPage}>
            <h1>Client Main Page</h1>
        </section>
    );
};

export default ClientMainPage;
